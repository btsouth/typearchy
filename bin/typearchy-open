#!/usr/bin/env bash
set -euo pipefail
# Compatibility for existing desktop registrations. All new launches use the app.
installed="$HOME/.local/bin/typearchy"
if [[ -x "$installed" ]]; then
  exec "$installed" "$@"
fi
exec "$(dirname "$(readlink -f "$0")")/typearchy" "$@"
